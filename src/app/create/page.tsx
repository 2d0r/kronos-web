import CreateTask from '../ui/tasks/create-task';

export default async function Page() {

    return (
        <main>
            <h2 className='text-primary text-center'></h2>
            {/* <CreateTask mindsets={mindsets} statuses={statuses}/> */}
        </main>
    )
}